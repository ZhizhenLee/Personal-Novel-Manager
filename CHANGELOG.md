# 更新日志 (Changelog)

本项目的变更将记录于此文件，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [1.0.0] - 2026-09-01

### 新增
- 三栏式内容管理：左侧主要文件夹（正文/草稿/角色库/世界观/章节标题列表/章节简介汇总/其他文件）、中间文件列表、右侧正文编辑器。
- 卡片式小说列表（封面缩略图 + 小说名），点击进入内容管理。
- 新建小说自动生成标准目录结构。
- 一键新建下一章（自动识别最大章节编号）。
- 文件预览：txt/md 正文、图片（封面/草稿）、带样式的 xlsx（背景色/边框/字体色/合并单元格/对齐）。
- 封面图片上传与缩略图显示。
- 自动保存开关（默认关闭，勾选后切换/关闭时自动落盘）。
- 可配置小说根目录路径，持久化记忆。

### 变更
- 「导入RAR覆盖小说目录」：用 RAR 内容整体覆盖某个小说（支持 WinRAR/7-Zip 解压，解压软件路径可配置并记忆）。
- 「导出小说为RAR文件」：将整个小说打包为 `小说名.rar`（可选排除 `_备份`）。
- 移除旧的单文件导入功能。
- 修复：进入小说内部时导入 RAR 完成后主界面变空白（导入后统一回到主列表）。

### 技术
- Python 3.13 + Tkinter，PyInstaller `--onefile --windowed` 打包为单文件免安装 exe。
